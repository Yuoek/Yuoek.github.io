@import '_layout';
@import '_header';
@import '_menu';
@import '_sub-menu';
@import '_sidebar';

.main-inner {
  background: var(--content-bg-color);
  box-shadow: $box-shadow-inner;
  padding: $content-desktop-padding;

  +tablet-mobile() {
    padding: 20px;
  }
}

// Sub-menu(s).
.sub-menu {
  border-bottom: 1px solid $table-border-color;
}

.post-block:first-of-type {
  padding-top: 20px;
}

.pagination {
  +mobile() {
    margin-bottom: 10px;
  }
}

body {

    background:url(/images/L2.png);

    background-repeat: no-repeat;

    background-attachment:fixed; //不重复

    background-size: cover;      //填充

    background-position: 30% 50%;
	mix-blend-mode: multiply;	


}

.main-inner {
    background-color: rgba(255, 255, 255, 0);
}


.header-inner{
	opacity: 0.9;
}
.main-inner { 
   // margin-top: 60px;
   // padding: 60px 60px 60px 60px;

    opacity: 0.8;
}

.post-block{
        background-color: rgba(255, 255, 255, 0);
        margin-top: 24px;
        margin-bottom: 24px;
        padding: 20px;
        border-radius: 30px 30px 30px 30px;
        box-shadow: 2px 2px 2px 2px rgba(0,0,0,0), 2px 2px 2px -2px rgba(0,0,0,0), 2px 2px 2px 2px rgba(0,0,0,0), 2px 2px 2px 2px rgba(0,0,0,0);
}